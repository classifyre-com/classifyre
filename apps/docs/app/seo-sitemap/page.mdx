---
title: SEO and Sitemap
description: "SEO baseline mirrored from Banandre: metadata, JSON-LD, robots, and sitemap."
---

import { Card, CardContent, CardHeader, CardTitle } from "@workspace/ui/components"

# SEO and Sitemap

This docs app mirrors the same SEO structure used in Banandre:

- rich `metadata` in layout
- JSON-LD scripts in `<Head>`
- `next-sitemap` generation after static build
- `robots.txt` generated from sitemap config

`next-sitemap.config.js` sets per-route priority and change frequency while
respecting static export output.

<Card className="panel-card mt-8">
  <CardHeader>
    <CardTitle>Commands</CardTitle>
  </CardHeader>
  <CardContent>

```sh
bun --filter @classifyre/docs run build
# postbuild automatically runs next-sitemap
```

  </CardContent>
</Card>
